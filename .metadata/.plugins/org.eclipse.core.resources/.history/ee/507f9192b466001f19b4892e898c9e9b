package algorithm.quiz.ch05;

// 유클리드 호제법을 재귀메서드 없이
public class Q2 {
	static int gcd(int x, int y) {
		int a = x; 
		int b = y;
		
		while (true) {
			if (b == 0)
				return a;
			else {
				a = y;
				b = x % y;
			}
		}
	}
}
